datasets:
  # for each dataset specify the name, type, path, and separator in case of csv file

  - name: "countries"
    type: "csv"
    path: "../resources/datasets/countries/"
    origin: "test"

  - name: "players"
    type: "csv"
    path: "../resources/datasets/players/"
    origin: "test"